# عملگر توان

برای JavaScript عملگری پیشنهاد شده که توان‌گیری را مثل `Math.pow(..)` انجام می‌دهد. مثال:

```js
var a = 2;

a ** 4;			// Math.pow( a, 4 ) == 16

a **= 3;		// a = Math.pow( a, 3 )
a;				// 8
```

**نکته:** `**` عملاً همان چیزی است که در Python، Ruby، Perl و زبان‌های دیگر هم می‌بینید.
