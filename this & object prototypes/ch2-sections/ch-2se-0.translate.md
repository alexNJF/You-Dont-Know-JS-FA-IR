# شما جاوااسکریپت را نمی‌دانید: this و Object Prototypes - ویرایش اول
# حالا این معنا پیدا می‌کند!

در فصل ۱، تصورات غلط مختلف دربارهٔ `this` را کنار گذاشتیم و به‌جای آن یاد گرفتیم که `this` bindingی است که برای هر فراخوانی تابع ساخته می‌شود، کاملاً بر اساس **call-site** آن (نحوهٔ فراخوانی تابع).
